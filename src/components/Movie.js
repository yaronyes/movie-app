import React, {useState, useEffect}from 'react';

const Movie = (props) => {
    const { movie } = props;

    return (
        <div className="comp-movie">
            Movie
        </div>
    )
};

export default Movie;